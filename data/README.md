# 数据目录

- `raw/`：未经修改的原始数据，只添加、不覆盖。
- `interim/`：清洗、合并或特征构造过程中的中间数据。
- `processed/`：正式模型直接读取的稳定输入。

每个数据集应记录来源、许可、获取日期、字段含义、单位、缺失值、处理步骤和对应脚本。禁止提交队伍身份信息或未经授权的个人敏感数据。

CSV/TSV 默认作为可比较的文本由普通 Git 管理。单个表格明显增大（建议在 10 MB 前评估）时，先与队友确认是对该文件启用 Git LFS，还是只提交获取方法与 SHA-256 校验值，避免仓库历史持续膨胀。
