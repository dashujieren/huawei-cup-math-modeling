# 第三问：四项目标等权优化（自行运行）

以师兄的 80 箱 PASS 方案构造可行基线，然后在给定时间预算内尝试：①固定已选路线重排关键时刻；②开放现有紧凑组批及相邻服务区合并候选，联合重排运输和中继。候选必须再次通过独立连续通信、时限和资源验收，且四项归一化等权得分严格降低，才会被采用。有限候选和有限时间**不保证全局最优**。

## 在 PyCharm 里运行

1. Python 解释器选 `D:\Documents\ChatGPT\研究生数学建模比赛\D题\code\.venv\Scripts\python.exe`。此环境已安装 OR-Tools；Git 目录自带的 `.venv` 目前没有 OR-Tools，直接使用会在求解时失败。若要继续用 Git 目录的虚拟环境，先运行 `python -m pip install -r requirements.txt`。
2. Script path 选你正在使用的目录里的 `q3_1_optimize.py`。
3. Parameters 填：

   ```text
   --data-run "D:\Documents\ChatGPT\研究生数学建模比赛\D题\code\0_outputs\run_20260924_154122" --optimize-seconds 300
   ```

   Git 目录里同名数据副本目前与校验清单哈希不一致，所以必须显式使用上面这份校验通过的数据。首次运行需要重新认证中继站点，可能比 300 秒搜索预算长很多；之后相同代码、参数和数据会复用几何缓存。需要先做更短的尝试，可把 `300` 改成 `60`。

4. Working directory 设为脚本所在的 `code` 目录。运行中别把 `OPTIMAL`（可行性阶段）误读为四项目标全局最优。

## 命令行示例

```powershell
& "D:\Documents\ChatGPT\研究生数学建模比赛\D题\code\.venv\Scripts\python.exe" -B "D:\codex\huawei-cup-math-modeling\code\q3_1_optimize.py" --data-run "D:\Documents\ChatGPT\研究生数学建模比赛\D题\code\0_outputs\run_20260924_154122" --optimize-seconds 300
```

若要从工作区那份代码运行，只把脚本路径改成 `D:\Documents\ChatGPT\研究生数学建模比赛\D题\code\q3_1_optimize.py`，其余不变。

## 看结果

默认新结果写在脚本所在目录的 `3_outputs\2_equal_optimize\q3_opt1_..._table`，图表在同根目录的 `..._figure`；不会覆盖师兄原有 `1_optimize` 结果。先看 `Q3_运行摘要.json`：

- `status` 必须是 `PASS`，并且 `Q3_READY.txt` 存在，才可使用表格。
- `optimization.status = IMPROVED` 表示找到并验收了比本次基线更好的方案；`NO_IMPROVEMENT` 表示仍交付 PASS 基线，不代表不存在更优解。
- `optimization.baseline`、`optimization.final` 和 `equal_weight_score` 给出前后四项原值与得分；基线得分为 1，只有小于 1 才称改进。
- `_baseline` 和 `_candidate_*` 子目录保留了每阶段表格与验收证据。

可独立复验终稿（把目录换成实际输出）：

```powershell
& "D:\Documents\ChatGPT\研究生数学建模比赛\D题\code\.venv\Scripts\python.exe" -B "D:\codex\huawei-cup-math-modeling\code\q3_1_optimize.py" --data-run "D:\Documents\ChatGPT\研究生数学建模比赛\D题\code\0_outputs\run_20260924_154122" --validate-only "D:\codex\huawei-cup-math-modeling\code\3_outputs\2_equal_optimize\实际的_table目录"
```

轻量测试：运行同目录 `q3_5_equal_optimize_test.py`，它不会启动正式求解。原工作区旧版 `q3_1_optimize.py` 已备份为 `q3_1_optimize_pre_senior_20260925.py`，不需要上传这个备份。
