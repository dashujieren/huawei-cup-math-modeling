# 第三问历史文件

此目录保存旧等权实验、测试脚本及重复的时间优化入口，仅供核对历史结果；它们不是最终运行入口，移入归档后也不保证能从本目录直接运行。

第三问当前时间优化入口为上一级的 `q3_5_optimize.py`。`q3_1_optimize.py`、`q3_2_optimize.py`、`q3_3_optimize.py` 等仍是它的运行依赖，保留在 `code/`。已保存的 `3_outputs/` 结果未移动。
