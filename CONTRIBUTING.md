# 协作规范

## 分支与合并

- `main` 保存可运行、已检查的内容，不直接进行大范围试验性修改。
- 分支名使用 `类型/简短主题`，例如 `data/clean-weather`、`model/baseline`、`paper/abstract`。
- 合并前先同步 `main`，运行与改动相关的检查，并请至少一名队友审阅关键模型和论文结论。
- 不使用强制推送改写共享分支历史。

## 提交信息

提交信息使用 `范围: 动作`，常用范围包括：

- `data`：数据清洗、字段或来源记录
- `model`：模型、求解器或参数
- `eval`：验证、敏感性或稳健性分析
- `figure`：论文图表
- `paper`：论文内容和排版
- `docs`：说明与会议记录
- `chore`：工具和仓库维护

一次提交只完成一个可解释的改动，避免把数据、模型和论文的大量无关修改混在一起。

## 数据与实验

- `data/raw/` 中的文件只添加、不覆盖；清洗结果写入 `data/interim/` 或 `data/processed/`。
- 数据说明应记录来源、获取日期、字段、单位、缺失值和处理步骤。
- 正式实验应固定随机种子与参数，并在 `outputs/runs/` 使用唯一运行目录。
- 不静默覆盖历史结果；修正后的运行使用新的标识，并说明与旧结果的关系。
- Notebook 用于探索；进入论文的计算逻辑应整理到 `src/` 或 `scripts/`。

## 文件与安全

- 先检查许可和比赛规则，再提交第三方数据、论文全文或模型文件。
- 大型二进制文件遵循 Git LFS 配置；超出配额时提交获取说明和校验值。
- 令牌、密码、证书、个人身份材料和队伍报名信息不得进入 Git 历史。
- 发现敏感信息误提交时立即停止推送、通知全队并轮换相关凭据；若已进入 Git 历史，还要按团队确认的范围清理历史，不能只删除最新版本中的文件。

## 合并前检查

- 代码能在干净环境中按说明运行。
- 路径不依赖某位队员的绝对目录。
- 图表有标题、单位、图例和可追溯的数据来源。
- 论文中的数值与当前正式输出一致。
- 没有缓存、临时文件、密钥或不应分发的材料。
